import { createTheme } from '@material-ui/core/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#0052cc',
    },
    background: {
      main: '#edf2ff',
    },
  },
});

export default theme;